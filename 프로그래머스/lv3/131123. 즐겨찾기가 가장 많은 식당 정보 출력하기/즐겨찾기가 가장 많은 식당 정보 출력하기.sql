-- 코드를 입력하세요
SELECT DISTINCT A.FOOD_TYPE, A.REST_ID, A.REST_NAME, A.FAVORITES FROM REST_INFO A INNER JOIN (SELECT FOOD_TYPE, MAX(FAVORITES) AS RES FROM REST_INFO GROUP BY FOOD_TYPE) B ON A.FAVORITES = B.RES AND A.FOOD_TYPE = B.FOOD_TYPE ORDER BY FOOD_TYPE DESC